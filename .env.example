# Optional. Without a key, REELROOM fetches posters and metadata from Wikipedia/Wikidata.
# A key upgrades posters to TMDB's higher-resolution artwork.
# Get one at https://www.themoviedb.org/settings/api (free, requires an account).
VITE_TMDB_API_KEY=
